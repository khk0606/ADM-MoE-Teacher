# Teacher-v9.2 validator correction

Read-only correction for CPU-versus-CUDA listwise reduction tolerance plus a
deterministic JSON evidence summary.  It does not rerun CUDA, change the saved
maps, change FAIL to PASS, authorize training, or modify the model.
